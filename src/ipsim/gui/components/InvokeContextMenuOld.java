package ipsim.gui.components;
/*
import ipsim.awt.*;
import ipsim.gui.*;
import ipsim.network.connectivity.*;
import ipsim.network.*;

import javax.swing.*;

public class InvokeContextMenu
{
	public static void invokeContextMenu(final JPopupMenu menu,final Network network,final NetworkView view,final PacketSource component)
	{
		final Point centre=ObjectRenderer.centre(network,component);
		menu.show(view,(int)(centre.x()*network.zoomLevel.get()),(int)(centre.y()*network.zoomLevel.get()));
	}
}
*/