package ipsim.network.connectivity.card;
/*
import ipsim.*;
import ipsim.awt.*;
import static ipsim.lang.PositionOrParent.*;
import ipsim.network.connectivity.computer.*;

public class CardFactory
{
	public static Card newCardConnectedTo(final Computer computer, final int x, final int y)
	{
		Point point=new Point(x, y);
		final Card card=new Card(false, position(point));
		card.setPositionData(0, Caster.asNotNull(parent(computer)));
		return card;
	}
}
*/