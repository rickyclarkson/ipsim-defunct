package ipsim.tests;
/*
import static ipsim.network.NetMask.netMask;
import fpeas.lazy.Lazy;

public class NetMaskTest
{
	public static Lazy<Boolean> testEquality()
	{
		return new Lazy<Boolean>()
		{
			public Boolean invoke()
			{
                            return netMask(0).get().equals(netMask(0).get());
			}

			@Override
			public String toString()
			{
				return "testEquality";
			}
		};
	}
}
*/